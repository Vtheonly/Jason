// This file is a historical duplicate of ../../config/schema_validator.js.
// The CLI and the web gateway both import from `../config/schema_validator.js`.
// This stub exists for backwards-compatibility with documentation that may
// reference the old path; it simply re-exports the canonical implementation.

export { SchemaValidator } from '../config/schema_validator.js';
